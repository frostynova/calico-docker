general:
  artifacts:
    - "dist"
machine:
  services:
    - docker
dependencies:
  override:
    - echo "hi"
test:
  override:
    - pushd build_calicoctl && ./create_binary.sh && popd 
    - sudo build_node.sh 
    - sudo tests/fv/mainline.sh 
